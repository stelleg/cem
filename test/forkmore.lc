fork = \child.\parent.(sys_fork 0 (0 =) child parent)

main = [
  (print "Parent thread begins") then
  [fork print "This is a forked print"]
  [fork print "Another forked print"]
  (print "Parent thread ends")
]
