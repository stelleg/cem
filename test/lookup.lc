# Looks up key/value pair
dict = {dict = (Y \dict.\acc.\k.(nil? k acc \v.(dict (: (Pair k v) acc))))} (dict ;) 
env = (dict "key" "value" 
            "newkey" "newvalue"
            "thirdkey" "thirdvalue" ;)
val = (lookup "newkey" strcmp env "newkey not found!" \x.x)

main = (print val)
