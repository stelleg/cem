(\Y.
  (\true.
    (\false.
      (\zero.
        (\one.
          (\iszero.
            (\mult.
              (\pred.
                (\sub.
                  (\eight.
                    (\sixteen.
                      (\lte.
                        (\tak.
                          (iszero ((sub (((tak sixteen) eight) zero)) one))
                        (Y \tak.\x.\y.\z.((((lte x) y) 
                          z)
                          (((tak (((tak (pred x)) y) z))
                                 (((tak (pred y)) z) x))
                                 (((tak (pred z)) x) y)))))
                      \n.\m.(iszero ((sub n) m))) 
                    ((mult eight) \f.\x.(f (f x))))
                  \f.\x.(f (f (f (f (f (f (f x))))))))
                \m.\n.((n pred) m))
              \n.\f.\x.(((n \g.\h.(h (g f))) \u.x) \u.u))
            \m.\n.\f.(m (n f)))
          \n.((n \z.false) true))
        \f.\x.(f x))
      \f.\x.x)
    \x.\y.y)
  \x.\y.x)
\g.(\x.(g (x x)) \x.(g (x x))))
