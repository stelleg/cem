divs = \n.\x.(x % n != 0)
thefilter = \l.(filter [divs head l] (tail l))
primes = [(map head) (iterate thefilter) from 2]

main = (>> (putStr "Enter # primes: ")
       (>>= (return "1500") \n.
       (for (take (readInt n) primes) (∘ print showInt))))
